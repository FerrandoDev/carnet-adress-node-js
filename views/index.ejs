<%- include('header') -%>
<div class="container">
    <ul>
    <% for (const person of persons) {%>
    <li class="person">
        <a href="/kitten/<%= person.id %>"> <p><%= person.first_name %> <%= person.last_name %> </p> </a>
    </li>
    <% } %>
    </ul>
</div>
</body>
</html>